\chapter{Evaluation}
\label{ch:evaluation}

\section{Correctness}
\label{sec:testing}

Kotlin codebase already contains extensive tests for coroutines. We can separate them into
two levels.

\subsection*{Intrinsic semantics tests}

These tests verify the behavior of the lowest-level coroutine
primitives like \startCoroutineUninterceptedOrReturn and \suspendCoroutineUninterceptedOrReturn.
They also cover low-level coroutine APIs built directly on top of them like \texttt{startCoroutine}
and \texttt{suspendCoroutine}

The tests are located in the Kotlin repository at
\btexttt{compiler/testData/codegen/box/coroutines/intrinsicSemantics}

You can find an example of what kind of tests are in the \texttt{intrinsicSemantics} folder in Listing~\ref{lst:intrinsic-semantics-example} in the Appendix~\ref{ch:appendix}).

\subsection*{Box tests for coroutines}

This is an extensive collection of tests that verifies that coroutines work correctly when used
together with other Kotlin features.
Some of the things that are tested are:
\begin{itemize}
    \item \textbf{Control flow} (tests in \texttt{controlFlow/} directory): testing coroutines
    with \texttt{if}, \texttt{while}, \texttt{try-catch-finally}, and other control flow structures.
    \item \textbf{Default and variadic parameters} (e.g., \btexttt{defaultParametersInSuspend.kt},
    \btexttt{varargCallFromSuspend.kt}): testing suspend functions with default or variadic arguments.
    \item \textbf{Exception handling} (e.g., \texttt{handleException.kt}, \texttt{simpleException.kt}): testing
    that exceptions thrown inside coroutines are correctly passed to the completion continuation or
    handled by \texttt{try-catch} blocks.
\end{itemize}

As an example, below is a short test that verifies coroutine behavior with an \texttt{if} expression:

\begin{lstlisting}[language=Kotlin, caption={Example from controlFlow\_if1.kt}]
suspend fun s1(): Int = suspendCoroutineUninterceptedOrReturn { x ->
    x.resume(42)
    COROUTINE_SUSPENDED
}

fun f1(): Int = 117
fun f2(): Int = 1
fun f3(x: Int, y: Int): Int = x + y

fun box(): String {
    var result = 0
    suspend {
        result = f3(if (f1() > 100) s1() else f2(), 42)
    }.startCoroutine(object : Continuation<Unit> {
        override val context = EmptyCoroutineContext
        override fun resumeWith(result: Result<Unit>) {}
    })

    if (result != 84) return "fail: $result"
    return "OK"
}
\end{lstlisting}

The tests are located in the Kotlin repository at \btexttt{compiler/testData/codegen/box/coroutines}

\section{Size benchmark}
\label{sec:size-bench}

To evaluate the impact of the stack-switching implementation on binary size, a small Kotlin project generated by
the Kotlin Multiplatform Wizard~\cite{kotlin-multiplatform-wizard}
was compiled using three different compiler versions:

\begin{itemize}
    \item \textbf{Baseline}: Kotlin 2.3.10 (original implementation without stack-switching)
    \item \textbf{Stack-switching}: Base stack-switching implementation rebased to Kotlin 2.3.10
    \item \textbf{Optimized SS}: Stack-switching with immediate resume optimization rebased to Kotlin 2.3.10
\end{itemize}

For each compiler version, two distributions were generated:
\begin{itemize}
    \item \btexttt{wasmJsBrowserDevelopmentExecutableDistribution}: development build without optimizations
    \item \btexttt{wasmJsBrowserDistribution}: production build with optimizations
\end{itemize}

Table~\ref{tab:size-benchmark} shows the size of the compiled \btexttt{KotlinProject-composeApp.wasm} file in kilobytes
for each configuration.

\begin{table}[H]
\centering
\caption{Wasm binary size comparison (in KB)}
\label{tab:size-benchmark}
\begin{tabular}{|l|r|r|r|}
\hline
\textbf{Build Mode} & \textbf{Baseline} & \textbf{Stack-switching} & \textbf{Optimized SS} \\
\hline
Dev & 20,460.67 & 19,987.97 & 19,992.46 \\
Prod & 2,082.82 & 1,857.17 & 1,858.34 \\
\hline
\end{tabular}
\end{table}

Table~\ref{tab:size-percentage} shows the percentage change in binary size compared to the baseline. Negative values indicate a size reduction.

\begin{table}[H]
\centering
\caption{Size change compared to baseline (negative values indicate reduction)}
\label{tab:size-percentage}
\begin{tabular}{|l|r|r|r|}
\hline
\textbf{Build Mode} & \textbf{Baseline} & \textbf{Stack-switching} & \textbf{Optimized SS} \\
\hline
Dev & 20,460.67 KB & -2.3\% & -2.3\% \\
Prod & 2,082.82 KB & -10.8\% & -10.8\% \\
\hline
\end{tabular}
\end{table}

The results show that both stack-switching implementations achieve a reduction in binary size compared to the
baseline. The size reduction is bigger in optimized builds (approximately 10.8\%) compared to development builds
(approximately 2.3\%). The difference between the base stack-switching implementation and the optimized version is
negligible in terms of binary size, with both achieving similar reductions.

\section{Performance benchmarks}
\label{sec:performance-bench}

The performance of the implementation was measured by running benchmarks on low-level coroutine primitives.
The following cases are measured:

\begin{itemize}
    \item \btexttt{chainSuspensions.kt}: 20 coroutines are created and suspended. When the first coroutine is resumed, it
    suspends again, and in the suspend lambda the second coroutine is resumed. The second coroutine suspends and
    resumes the third coroutine in its suspend lambda. This continues on until the last coroutine is resumed, which
    records the fact that it was reached in a global variable. After that, all coroutines that were suspended so far
    are resumed so that they terminate properly.
    \item \btexttt{deepSuspensions.kt}: same setup as in \btexttt{chainSuspensions.kt}, but when the last coroutine
    in the chain is resumed, it also suspends and resumes 20 times.
    \item \btexttt{multipleSuspensions1.kt}: create one coroutine and suspend and resume it 50 times. A pattern
    with immediate resume inside suspend lambda is used.
    \item \btexttt{multipleSuspensions2.kt}: same as \btexttt{multipleSuspensions1.kt}, but the immediate resume pattern
    is not used. For each suspension the continuation is saved and resumed from the main context.
    \item \texttt{sequence.kt}: this benchmark is an exception as it's not a benchmark on low-level primitives. It's a
    benchmark for a Kotlin sequence builder, which is built on top of coroutines. For this test a sequence builder
    is used to compute the first 20 numbers from the Fibonacci sequence.
    \item \btexttt{startCoroutinePerformance.kt}: start 100 coroutines with \texttt{startCoroutine}
\end{itemize}

Three code versions were used for comparison:

\begin{itemize}
    \item \textbf{Baseline} (commit \texttt{0da05969}): The original implementation without stack-switching
    \item \textbf{Stack-switching} (commit \texttt{1ad2ba84}): Initial stack-switching implementation
    \item \textbf{Optimized SS} (commit \texttt{9c55cf92}): Stack-switching with immediate resume optimization
\end{itemize}

The benchmarks were executed in two compilation modes:

\begin{itemize}
    \item \textbf{dev mode}: Development build without optimizations
    \item \textbf{dce mode}: Production build with dead code elimination
\end{itemize}

Each benchmark was run for 30 iterations after 20 warmup iterations. The figures in this chapter
will display the average value across 30 iterations.

\subsection{V8 (Google Chrome)}

\subsubsection*{Raw results}

Tables~\ref{tab:benchmark-dev} and~\ref{tab:benchmark-dce} show the raw benchmark results for dev and dce modes respectively.

\begin{table}[H]
\centering
\caption{Benchmark results in dev mode (average time in microseconds, V8 runtime)}
\label{tab:benchmark-dev}
\begin{tabular}{|l|r|r|r|}
\hline
\textbf{Test} & \textbf{Baseline} & \textbf{Stack-switching} & \textbf{Optimized SS} \\
\hline
chainSusp & 20,900 & 115,500 & 113,100 \\
deepSusp & 29,100 & 109,466 & 124,433 \\
multiSusp1 & 10,600 & 17,466 & 10,500 \\
multiSusp2 & 9,266 & 17,666 & 25,300 \\
sequence & 6,900 & 36,066 & 22,800 \\
startCoroutine & 24,000 & 113,333 & 120,400 \\
\hline
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Benchmark results in dce mode (average time in microseconds, V8 runtime)}
\label{tab:benchmark-dce}
\begin{tabular}{|l|r|r|r|}
\hline
\textbf{Test} & \textbf{Baseline} & \textbf{Stack-switching} & \textbf{Optimized SS} \\
\hline
chainSusp & 20,600 & 100,166 & 106,200 \\
deepSusp & 26,766 & 114,633 & 115,466 \\
multiSusp1 & 9,866 & 16,500 & 10,300 \\
multiSusp2 & 9,400 & 16,266 & 19,966 \\
sequence & 5,266 & 13,266 & 13,133 \\
startCoroutine & 26,000 & 58,200 & 71,566 \\
\hline
\end{tabular}
\end{table}

\subsubsection*{Percentage Changes}

Table~\ref{tab:benchmark-percentage} presents the percentage changes relative to the baseline. Positive values indicate
performance regression (slower execution), while negative values indicate improvement (faster execution).

\begin{table}[H]
\centering
\caption{Percentage change compared to baseline (negative values indicate improvement)}
\label{tab:benchmark-percentage}
\begin{tabular}{|l|c|r|r|r|r|}
\hline
\textbf{Test} & \textbf{Mode} & \textbf{Baseline} & \textbf{Stack-switching} & \textbf{Optimized SS} & \textbf{Opt vs SS} \\
\hline
chainSusp & dev & 20,900 & +452.6\% & +441.1\% & -2.1\% \\
chainSusp & dce & 20,600 & +386.2\% & +415.5\% & +6.0\% \\
deepSusp & dev & 29,100 & +276.2\% & +327.6\% & +13.7\% \\
deepSusp & dce & 26,766 & +328.3\% & +331.4\% & +0.7\% \\
multiSusp1 & dev & 10,600 & +64.8\% & -0.9\% & -39.9\% \\
multiSusp1 & dce & 9,866 & +67.2\% & +4.4\% & -37.6\% \\
multiSusp2 & dev & 9,266 & +90.7\% & +173.0\% & +43.2\% \\
multiSusp2 & dce & 9,400 & +73.0\% & +112.4\% & +22.7\% \\
sequence & dev & 6,900 & +422.7\% & +230.4\% & -36.8\% \\
sequence & dce & 5,266 & +151.9\% & +149.4\% & -1.0\% \\
startCoroutine & dev & 24,000 & +372.2\% & +401.7\% & +6.2\% \\
startCoroutine & dce & 26,000 & +123.8\% & +175.3\% & +23.0\% \\
\hline
\end{tabular}
\end{table}

\subsubsection*{Visual Comparison}

Figures~\ref{fig:benchmark-dev} and~\ref{fig:benchmark-dce} provide a visual comparison of the benchmark results across
all three implementations.

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    ybar,
    bar width=7pt,
    width=\textwidth,
    height=8cm,
    ylabel={Average time (microseconds)},
    xlabel={Benchmark test},
    symbolic x coords={chainSusp,deepSusp,multiSusp1,multiSusp2,sequence,startCoroutine},
    xtick=data,
    x tick label style={rotate=45, anchor=east},
    legend style={at={(0.5,-0.4)}, anchor=north, legend columns=3},
    ymin=0,
    enlarge x limits=0.1,
]
\addplot coordinates {
    (chainSusp,20900)
    (deepSusp,29100)
    (multiSusp1,10600)
    (multiSusp2,9266)
    (sequence,6900)
    (startCoroutine,24000)
};
\addplot coordinates {
    (chainSusp,115500)
    (deepSusp,109466)
    (multiSusp1,17466)
    (multiSusp2,17666)
    (sequence,36066)
    (startCoroutine,113333)
};
\addplot coordinates {
    (chainSusp,113100)
    (deepSusp,124433)
    (multiSusp1,10500)
    (multiSusp2,25300)
    (sequence,22800)
    (startCoroutine,120400)
};
\legend{Baseline, Stack-switching, Optimized SS}
\end{axis}
\end{tikzpicture}
\caption{Benchmark comparison in dev mode (V8 runtime)}
\label{fig:benchmark-dev}
\end{figure}

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    ybar,
    bar width=7pt,
    width=\textwidth,
    height=8cm,
    ylabel={Average time (microseconds)},
    xlabel={Benchmark test},
    symbolic x coords={chainSusp,deepSusp,multiSusp1,multiSusp2,sequence,startCoroutine},
    xtick=data,
    x tick label style={rotate=45, anchor=east},
    legend style={at={(0.5,-0.4)}, anchor=north, legend columns=3},
    ymin=0,
    enlarge x limits=0.1,
]
\addplot coordinates {
    (chainSusp,20600)
    (deepSusp,26766)
    (multiSusp1,9866)
    (multiSusp2,9400)
    (sequence,5266)
    (startCoroutine,26000)
};
\addplot coordinates {
    (chainSusp,100166)
    (deepSusp,114633)
    (multiSusp1,16500)
    (multiSusp2,16266)
    (sequence,13266)
    (startCoroutine,58200)
};
\addplot coordinates {
    (chainSusp,106200)
    (deepSusp,115466)
    (multiSusp1,10300)
    (multiSusp2,19966)
    (sequence,13133)
    (startCoroutine,71566)
};
\legend{Baseline, Stack-switching, Optimized SS}
\end{axis}
\end{tikzpicture}
\caption{Benchmark comparison in dce mode (V8 runtime)}
\label{fig:benchmark-dce}
\end{figure}

\subsection{Reference Interpreter}

\subsubsection*{Raw results}

Tables~\ref{tab:benchmark-ref-dev} and~\ref{tab:benchmark-ref-dce} show the raw benchmark results for dev and dce modes respectively.

\begin{table}[H]
\centering
\caption{Benchmark results in dev mode (average time in microseconds, Reference Interpreter)}
\label{tab:benchmark-ref-dev}
\begin{tabular}{|l|r|r|r|}
\hline
\textbf{Test} & \textbf{Baseline} & \textbf{Stack-switching} & \textbf{Optimized SS} \\
\hline
chainSusp & 758,311 & 486,995 & 955,909 \\
deepSusp & 887,526 & 565,484 & 1,079,435 \\
multiSusp1 & 1,214,480 & 849,231 & 131,810 \\
multiSusp2 & 94,166 & 128,453 & 193,193 \\
sequence & 36,715 & 50,726 & 84,870 \\
startCoroutine & 310,222 & 435,650 & 672,933 \\
\hline
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Benchmark results in dce mode (average time in microseconds, Reference Interpreter)}
\label{tab:benchmark-ref-dce}
\begin{tabular}{|l|r|r|r|}
\hline
\textbf{Test} & \textbf{Baseline} & \textbf{Stack-switching} & \textbf{Optimized SS} \\
\hline
chainSusp & 617,381 & 375,790 & 778,879 \\
deepSusp & 777,220 & 409,710 & 796,007 \\
multiSusp1 & 1,040,099 & 918,748 & 86,925 \\
multiSusp2 & 61,413 & 75,988 & 126,874 \\
sequence & 27,183 & 32,015 & 55,099 \\
startCoroutine & 195,163 & 273,544 & 414,767 \\
\hline
\end{tabular}
\end{table}

\subsubsection*{Percentage Changes}

Table~\ref{tab:benchmark-ref-percentage} presents the percentage changes relative to the baseline. Positive values indicate
performance regression (slower execution), while negative values indicate improvement (faster execution).

\begin{table}[H]
\centering
\caption{Percentage change compared to baseline (negative values indicate improvement)}
\label{tab:benchmark-ref-percentage}
\begin{tabular}{|l|c|r|r|r|r|}
\hline
\textbf{Test} & \textbf{Mode} & \textbf{Baseline} & \textbf{Stack-switching} & \textbf{Optimized SS} & \textbf{Opt vs SS} \\
\hline
chainSusp & dev & 758,311 & -35.8\% & +26.1\% & +96.3\% \\
chainSusp & dce & 617,381 & -39.1\% & +26.2\% & +107.3\% \\
deepSusp & dev & 887,526 & -36.3\% & +21.6\% & +90.9\% \\
deepSusp & dce & 777,220 & -47.3\% & +2.4\% & +94.3\% \\
multiSusp1 & dev & 1,214,480 & -30.1\% & -89.1\% & -84.5\% \\
multiSusp1 & dce & 1,040,099 & -11.7\% & -91.6\% & -90.5\% \\
multiSusp2 & dev & 94,166 & +36.4\% & +105.2\% & +50.4\% \\
multiSusp2 & dce & 61,413 & +23.7\% & +106.6\% & +67.0\% \\
sequence & dev & 36,715 & +38.2\% & +131.2\% & +67.3\% \\
sequence & dce & 27,183 & +17.8\% & +102.7\% & +72.1\% \\
startCoroutine & dev & 310,222 & +40.4\% & +116.9\% & +54.5\% \\
startCoroutine & dce & 195,163 & +40.2\% & +112.5\% & +51.6\% \\
\hline
\end{tabular}
\end{table}

\subsubsection*{Visual Comparison}

Figures~\ref{fig:benchmark-ref-dev} and~\ref{fig:benchmark-ref-dce} provide a visual comparison of the benchmark results across
all three implementations.

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    ybar,
    bar width=7pt,
    width=\textwidth,
    height=8cm,
    ylabel={Average time (microseconds)},
    xlabel={Benchmark test},
    symbolic x coords={chainSusp,deepSusp,multiSusp1,multiSusp2,sequence,startCoroutine},
    xtick=data,
    x tick label style={rotate=45, anchor=east},
    legend style={at={(0.5,-0.4)}, anchor=north, legend columns=3},
    ymin=0,
    enlarge x limits=0.1,
]
\addplot coordinates {
    (chainSusp,758311)
    (deepSusp,887526)
    (multiSusp1,1214480)
    (multiSusp2,94166)
    (sequence,36715)
    (startCoroutine,310222)
};
\addplot coordinates {
    (chainSusp,486995)
    (deepSusp,565484)
    (multiSusp1,849231)
    (multiSusp2,128453)
    (sequence,50726)
    (startCoroutine,435650)
};
\addplot coordinates {
    (chainSusp,955909)
    (deepSusp,1079435)
    (multiSusp1,131810)
    (multiSusp2,193193)
    (sequence,84870)
    (startCoroutine,672933)
};
\legend{Baseline, Stack-switching, Optimized SS}
\end{axis}
\end{tikzpicture}
\caption{Benchmark comparison in dev mode (Reference Interpreter)}
\label{fig:benchmark-ref-dev}
\end{figure}

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    ybar,
    bar width=7pt,
    width=\textwidth,
    height=8cm,
    ylabel={Average time (microseconds)},
    xlabel={Benchmark test},
    symbolic x coords={chainSusp,deepSusp,multiSusp1,multiSusp2,sequence,startCoroutine},
    xtick=data,
    x tick label style={rotate=45, anchor=east},
    legend style={at={(0.5,-0.4)}, anchor=north, legend columns=3},
    ymin=0,
    enlarge x limits=0.1,
]
\addplot coordinates {
    (chainSusp,617381)
    (deepSusp,777220)
    (multiSusp1,1040099)
    (multiSusp2,61413)
    (sequence,27183)
    (startCoroutine,195163)
};
\addplot coordinates {
    (chainSusp,375790)
    (deepSusp,409710)
    (multiSusp1,918748)
    (multiSusp2,75988)
    (sequence,32015)
    (startCoroutine,273544)
};
\addplot coordinates {
    (chainSusp,778879)
    (deepSusp,796007)
    (multiSusp1,86925)
    (multiSusp2,126874)
    (sequence,55099)
    (startCoroutine,414767)
};
\legend{Baseline, Stack-switching, Optimized SS}
\end{axis}
\end{tikzpicture}
\caption{Benchmark comparison in dce mode (Reference Interpreter)}
\label{fig:benchmark-ref-dce}
\end{figure}

\subsection{Analysis}

TODO: analyze benchmark results

\section{Summary}
\label{sec:evaluation-summary}

TODO: summarize benchmark results, both performance and size.