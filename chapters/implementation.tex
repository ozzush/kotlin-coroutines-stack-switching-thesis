\chapter{Implementation}
\label{ch:implementation}

\section{Primitives correspondence}

To implement Kotlin coroutines using Wasm Stack-switching proposal we have to implement the primitives
discussed in Section~\ref{sec:kotlin-coroutines}. Kotlin coroutine primitives can be mapped to the Wasm Stack-switching
primitives pretty closely:
\begin{itemize}
    \item \textbf{\Continuation} maps to Wasm continuation objects.

    \item \textbf{\createCoroutineUnintercepted} maps to \contnew.

    \item \textbf{\startCoroutineUninterceptedOrReturn} maps to \contnew followed by \wasmresume.

    \item \textbf{\suspendCoroutineUninterceptedOrReturn} maps to the \wasmsuspend instruction with some additional
    logic to execute suspend lambda (the \texttt{block} argument that is passed to \suspendCoroutineUninterceptedOrReturn).
    \item \textbf{\texttt{Continuation::resumeWith}} maps to the \wasmresume instruction.
\end{itemize}

This correspondence can be observed more closely in Figure~\ref{fig:primitives-correspondence}.

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \centering
        \resizebox{\textwidth}{!}{\input{diagrams/coroutine-primitives}}
        \caption{Kotlin coroutine primitives}
        \label{subfig:kotlin-primitives}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.48\textwidth}
        \centering
        \resizebox{\textwidth}{!}{\input{diagrams/stack-switching-primitives}}
        \caption{Wasm Stack-switching primitives}
        \label{subfig:wasm-primitives}
    \end{subfigure}
    \caption{Comparison between Kotlin and Wasm coroutine primitives}
    \label{fig:primitives-correspondence}
\end{figure}

Unfortunately, there is one crucial difference in how suspension is handled in Kotlin, which makes it not so obvious how to
implement it in Wasm. In Kotlin the function \suspendCoroutineUninterceptedOrReturn
accepts a lambda which either returns some result or suspends the execution. This lambda accepts an argument: current
continuation. Resuming this continuation will resume the function. The intention of providing this argument is
for the suspension lambda to contain code that saves the continuation in some variable. Then, in case
suspension really happens, the function will be resumed by resuming this saved continuation.

For example, consider this simple suspend function, that adheres to the intention behind suspension lambdas:

\begin{lstlisting}[language=Kotlin, basicstyle=\footnotesize\ttfamily]
var savedContinuation: Continuation<String>? = null

suspend fun suspendHere(): String = 
    suspendCoroutineUninterceptedOrReturn { cont ->
        // Save continuation before suspending
        savedContinuation = cont
        // Indicate suspension occurred
        COROUTINE_SUSPENDED
    }
\end{lstlisting}

In this code, the continuation \texttt{cont} is available inside the lambda and can be saved to
\texttt{savedContinuation} before the function actually suspends. Later, calling
\texttt{savedContinuation?.resume("result")} will resume the suspended function.

However, there is a more subtle case that must also be supported: the continuation can be resumed immediately inside
the suspension lambda. Although this pattern is unusual in user code, it is not prohibited and is covered by Kotlin's
semantics tests. The pattern is illustrated by the following example:

\begin{lstlisting}[language=Kotlin, basicstyle=\footnotesize\ttfamily]
suspend fun suspendFun(): String =
    suspendCoroutineUninterceptedOrReturn { cont ->
        cont.resume("OK")      // Resume immediately
        COROUTINE_SUSPENDED    // Still indicate suspension
    }

fun test() {
    var completionResult: String? = null
    val result = suspendFun.startCoroutineUninterceptedOrReturn(
        object : Continuation<String> {
            override val context = EmptyCoroutineContext
            override fun resumeWith(value: Result<String>) {
                completionResult = value.getOrThrow()
            }
        }
    )
    // result == COROUTINE_SUSPENDED
    // completionResult == "OK"
}
\end{lstlisting}

Essentially, in Kotlin the continuation object has to be available and fully functional before we even know whether
a function suspends or not. On the contrary, in Wasm we only get the continuation object after the function actually
suspends. This difference can be observed in Figure~\ref{fig:primitives-correspondence-r}. The figure highlights the
mismatch in when continuation is required in Kotlin and when it becomes available in Wasm.

Next we will discuss two ways to solve this problem.

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \centering
        \resizebox{\textwidth}{!}{\input{diagrams/coroutine-primitives-r}}
        \caption{Kotlin coroutine primitives}
        \label{subfig:kotlin-primitives-r}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.48\textwidth}
        \centering
        \resizebox{\textwidth}{!}{\input{diagrams/stack-switching-primitives-r}}
        \caption{Wasm Stack-switching primitives}
        \label{subfig:wasm-primitives-r}
    \end{subfigure}
    \caption{Continuation availability problem between Kotlin and Wasm}
    \label{fig:primitives-correspondence-r}
\end{figure}

\section{First implementation}
\label{sec:base-implementation}

\begin{figure}[H]
    \centering
    \resizebox{\textwidth}{!}{\input{diagrams/coroutines-base-impl}}
    \caption{First implementation idea}
    \label{fig:base-implementation}
\end{figure}

The first implementation idea is shown in Figure~\ref{fig:base-implementation}. The control flow is as follows.

\startCoroutineUninterceptedOrReturn is called on a suspend function. The first thing we do is create a Wasm
continuation from the suspend function.

\WasmContinuation is a class that inherits from \CoroutineImpl and represents a coroutine that
works on a Wasm continuation object. The one method in \CoroutineImpl that needs to
be implemented in \WasmContinuation is \texttt{CoroutineImpl::doResume()}. When we call
\texttt{WasmContinuation::resumeWith}, it in turn calls \doResume, which contains the core logic for resuming
a coroutine.

In \doResume we call the Wasm instruction to resume the continuation. The type of Wasm continuations handled
in \WasmContinuation is always the same: they have to be resumed with an object of type \texttt{Any?}.
When \WasmContinuation is resumed in \startCoroutineUninterceptedOrReturn, the resume value
will be a completion. As discussed in Section~\ref{subsec:suspend-funs}, completion is a parameter added
to all suspend functions during compilation. When \WasmContinuation is resumed outside
\startCoroutineUninterceptedOrReturn, the resume value will be the value provided with the method
\resumeWith

When a suspend function is being executed, it may at some point call \suspendCoroutineUninterceptedOrReturn.
\suspendCoroutineUninterceptedOrReturn has one argument that we will call suspend lambda. Suspend lambda needs to be
executed to decide whether we should suspend or not, but to do so it requires that we pass it a continuation.
This continuation should represent the rest of the current suspend function. In the context of the suspend function
that's currently being executed, we can't provide the necessary continuation. Instead, we can suspend, and with
\wasmsuspend instruction we can pass the lambda that needs to be executed to the main context, where the Wasm
continuation we need becomes available.

The \wasmsuspend instruction puts the execution back into the body of \doResume. At the top of the stack there will be
a suspend lambda to be executed and the Wasm continuation object we need for it. So, the next step is to create a
\WasmContinuation from the Wasm continuation and execute the suspend lambda with it. If the lambda returns
\COROUTINESUSPENDED, that means that the coroutine really suspended and it's time to return. If the lambda returns
another value, that means that we have to keep executing the suspend function. So, we resume the new continuation with
the value returned from the lambda.

\section{Immediate resume optimization}
\label{sec:optimization}

\begin{figure}[H]
    \centering
    \resizebox{\textwidth}{!}{\input{diagrams/coroutines-optimization}}
    \caption{Immediate resume optimization}
    \label{fig:optimization-impl}
\end{figure}

The optimized implementation idea is shown in Figure~\ref{fig:optimization-impl}.

Behind this optimization is a heuristic. What is the actual meaning of resuming a continuation
that is passed to the suspend lambda? It means that the suspension lambda requests immediate continuation of the suspended computation.
With this in mind, we can adopt the following course of action: we call suspend lambda with
a \WasmContinuation object, that doesn't have an actual Wasm continuation inside. If the lambda
saves this \WasmContinuation, that is fine – we will tie it to a Wasm continuation later.
If the lambda resumes this \WasmContinuation, we will record the value passed to
\resumeWith, but otherwise we will do nothing. Once the lambda completes, we will check whether
\WasmContinuation was resumed, and if it was, we will get the resume value out of it and act
as if that was the value that the lambda returned.
